name: Build MMDB

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 0"
  push:
    branches:
      - main
    paths-ignore:
      - ".gitignore"
      - "LICENSE*"
      - "*.md"

jobs:
  build:
    name: Build MMDB
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codebase
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: ./go.mod

      - name: Set variables
        run: |
          echo "TAG_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "RELEASE_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
        shell: bash

      - name: Install geoip CLI from source
        run: |
          # Устанавливаем Loyalsoldier/geoip CLI
          go install -v github.com/Loyalsoldier/geoip@latest
          
          # Проверяем установку
          ~/go/bin/geoip version
          ~/go/bin/geoip list

      - name: Download Country MMDB from Loyalsoldier/geoip
        run: |
          mkdir -p output
          # Скачиваем готовый MMDB файл с последнего релиза
          curl -L -s "https://github.com/Loyalsoldier/geoip/releases/latest/download/Country-asn.mmdb" -o ./output/Country-source.mmdb
          
          # Проверяем размер (должен быть > 100KB)
          size=$(stat -f %s ./output/Country-source.mmdb)
          echo "Размер скачанного MMDB: $size байт"
          if [ $size -lt 100000 ]; then
            echo "ОШИБКА: Скачанный файл слишком маленький!"
            exit 1
          fi

      - name: Build custom MMDB
        run: |
          # Используем установленый geoip CLI
          ~/go/bin/geoip convert -c ./config.json

      - name: Verify mmdb files
        run: |
          cd ./output || exit 1
          go install github.com/maxmind/mmdbverify@latest
          for name in *.mmdb; do
            $(go env GOPATH)/bin/mmdbverify -file ${name}
          done
        continue-on-error: true

      - name: Generate sha256 checksum
        run: |
          cd ./output || exit 1
          for file in *.mmdb *.txt; do
            [ -f "$file" ] && sha256sum ${file} > ${file}.sha256sum
          done

      - name: Git push assets to "release" branch
        run: |
          cd output || exit 1
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release
          git add -A
          git commit -m "${{ env.RELEASE_NAME }}" || exit 0
          git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f -u origin release || exit 0

      - name: Upload files to GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: ./output/*
          tag_name: ${{ env.TAG_NAME }}
          name: ${{ env.RELEASE_NAME }}
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
